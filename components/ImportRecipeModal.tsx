import React from 'react';
import { View } from 'react-native';

// Import feature removed: provide a tiny stub so accidental imports keep compiling.
export const ImportRecipeModal: React.FC = () => {
	return <View />;
};

export default ImportRecipeModal;
