import { Spacing } from '@/constants/spacing';

// Shared padding that keeps scrollable content clear of persistent onboarding buttons.
export const ONBOARDING_SCROLL_BOTTOM_INSET = Spacing.xxxl + Spacing.xl;

